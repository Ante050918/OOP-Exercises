#ifndef HEADERVJEZBA3_H
#define HEADERVJEZBA3_H
#include <algorithm>
#include <vector>
#include <string>
using namespace std;
void input_vector1(vector <int> &vect, int &n);
int input_vector2(vector <int> &vect1, int &a, int &b);
void print_vector(vector <int>& vect, vector <int>& vect1, int& n, int& p);
void vector3(vector <int>& vect2, vector <int>& vect1, vector <int>& vect, int& n, int& p);
void sort_vector(vector <int>& vect);
void delete_element(vector <int>& vect);
int cnt_appearences(string &str, string &substr);
void reverse_and_sort();
#endif // HEADERVJEZBA3_H
